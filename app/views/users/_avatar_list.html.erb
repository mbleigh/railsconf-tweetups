  <ul class='avatars'>
    <% for user in users %>
      <li><%= link_to image_tag(user.profile_image_url, :width => 50, :height => 50), user_path(user), :title => "#{user.name} (@#{user.login})" %></li>
    <% end %>
  </ul>
